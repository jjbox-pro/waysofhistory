Включение флагов для дотошной компиляции

Файл куда добавить: ...\waysofhistory\platforms\android\build.gradle

allprojects {
    gradle.projectsEvaluated {
        tasks.withType(JavaCompile) {
            options.compilerArgs << "-Xlint:unchecked" << "-Xlint:deprecation"
        }
    }
}